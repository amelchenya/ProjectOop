<h1>Все фильмы</h1>
<h3>@Model.currCategory</h3>
<div class="row mt-5 mb-2">
    @{
        foreach (Films film in Model.allFilms)
        {
            @Html.Partial("AllFilms", film)
        }
    }
</div>